all:
  children:
    eks_nodes:
      hosts:
        node1:
          ansible_host: "{{ lookup('env', 'NODE_IP') }}"
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/eks-nodes.pem